## Running the VAE Training

>- navigate to the `jaxued/vae` directory in the command line
>- edit `"working_path"` entry in the `vae_train_config.yml` file
>- run `sample_envs.py` to generate random initial environments
>- run `train_vae.py`