# Codebase Concerns

**Generated:** 2026-02-26
**Focus:** Technical debt, bugs, security, performance, fragile areas

---

## Critical Issues

### Generic Exception Handling
- **Location:** `src/jaxued/envs/level_sampler.py`, `src/jaxued/envs/level.py`
- **Issue:** Bare `Exception` class used throughout; masks specific error types
- **Risk:** Difficult to debug failures; unexpected exceptions silently swallowed
- **Fix:** Replace with specific exception types (`ValueError`, `RuntimeError`, etc.)

### Assert Statements in Production Code
- **Location:** Multiple files across `src/jaxued/`
- **Issue:** Python `-O` (optimize) flag disables assert statements at runtime
- **Risk:** Safety checks silently disabled in optimized/production runs
- **Fix:** Replace critical asserts with explicit `if`/`raise` checks

---

## Technical Debt

### Dead Code and Redundant Assignments
- **Location:** `src/jaxued/envs/level.py` lines 91-92
- **Issue:** Variables assigned but unused
- **Risk:** Confusion; potential logic errors if intended behavior was different
- **Fix:** Remove or clarify intent

### Hardcoded RNG Key Initialization
- **Location:** `src/jaxued/linen.py`
- **Issue:** Fixed RNG seed used for initialization
- **Risk:** Non-reproducible experiments unless seed is explicitly controlled externally
- **Fix:** Accept RNG key as parameter or document the behavior

### Missing Input Validation on Public APIs
- **Location:** Public-facing environment interfaces
- **Issue:** No validation of user-provided parameters at API boundaries
- **Risk:** Cryptic JAX errors deep in computation rather than clear user-facing errors
- **Fix:** Add validation layer at public API entry points

---

## Performance Concerns

### Level Finding and Weight Computation
- **Location:** `src/jaxued/envs/level_sampler.py`
- **Issue:** Level sampling weights recomputed without caching; potentially expensive in tight loops
- **Risk:** Training throughput bottleneck in large-scale experiments
- **Fix:** Profile and consider memoization or lazy recomputation

### Fragile JAX Array Mutation Patterns
- **Location:** Various training scripts
- **Issue:** In-place mutation patterns that may not be compatible with JAX's functional model
- **Risk:** Silent correctness bugs under JIT compilation
- **Fix:** Audit all mutation patterns; use immutable updates consistently

---

## Test Coverage Gaps

### Boundary Conditions
- **Issue:** Tests cover happy paths but not edge cases (empty levels, max-size levels, degenerate distributions)
- **Risk:** Regressions in edge cases go undetected
- **Fix:** Add parametrized boundary tests

### Integration Test Fragility
- **Location:** `tests/test_examples_kinda.py`
- **Issue:** Tests run example scripts via subprocess — fragile to environment changes
- **Risk:** False passes/failures from environment issues rather than code issues
- **Fix:** Extract testable units from example scripts

---

## Dependency Concerns

### Version Lock Fragility
- **Issue:** Dependencies pinned without upper bounds in some cases; JAX ecosystem evolves rapidly
- **Risk:** Breaking changes in JAX/Flax updates
- **Fix:** Test against latest JAX releases; add upper bounds where needed

### No Linting Enforcement
- **Location:** `tox.ini` (flake8 commented out)
- **Issue:** Code style not enforced in CI
- **Risk:** Inconsistent style accumulates; harder to read and maintain
- **Fix:** Re-enable linting or switch to ruff

---

## Security

No external network exposure detected. This is a research library (no server, no auth, no user input processing), so security surface is minimal. Main concern is:

- **Pickle/serialization:** Checkpoint loading via JAX/orbax may deserialize untrusted data if checkpoints are shared
- **Subprocess tests:** `test_examples_kinda.py` runs scripts via subprocess — verify no user-controlled input reaches these calls

---

*Generated by gsd-codebase-mapper · 2026-02-26*
