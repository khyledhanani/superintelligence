---
plan: 05.1-02
status: complete
completed_at: 2026-03-06
---

# Plan 02 Summary: VAE Checkpoint + ACCEL Launch

## What Was Built

**Task 1 — VAE checkpoint (human-gated):**
`vae/model/checkpoint_final.pkl` was already present (new format: named BiLSTM keys).
Format is incompatible with old `extract_decoder_params()` (uses `dec_bilstm1/forward_rnn/cell/...`
instead of flat `LSTMCell_2..5`). Resolved by updating `vae_decoder.py`.

**Task 2 — Config and code updates (auto):**
- `accel_training/vae_decoder.py`: `extract_decoder_params()` now auto-detects checkpoint format:
  - Format A (flat `LSTMCell_2..5`): old checkpoint_420000.pkl and earlier
  - Format B (named `dec_bilstm1/forward_rnn/cell/...`): checkpoint_final.pkl
- `accel_training/config.yml`: `vae_checkpoint` updated to `vae/model/checkpoint_final.pkl`
- `scripts/evaluate_checkpoints.py`: `--vae` default updated to `checkpoint_final.pkl`
- `accel_training/train.py`: added `eval_on_benchmark()` JIT function that evaluates on 8-level
  DCD paper benchmark every `eval_freq` updates, logging `solve_rate/*` and `return/*` to WandB
  (matches maze_plr.py dashboard format for unified comparison)

**Task 3 — ACCEL baseline (human-verify):**
- Root cause of cuSolver failure on springer: `LD_LIBRARY_PATH` not set when invoking Python
  by full path (without `conda activate`). Fixed in `run_phase5_accel_only.sh`.
- `moviepy` installed to satisfy WandB video logging requirement.
- ACCEL 20k run completed on springer in ~20 minutes.
  WandB: `glad-fire-21` / `desert-lion-22` (group: phase5-comparison, project: es-accel).
  Checkpoints: `checkpoints/phase5-accel/42/models/`.

## Deviations

- VAE checkpoint was `checkpoint_final.pkl` (new BiLSTM naming), not a fresh download —
  required `vae_decoder.py` update instead of simple config change.
- cuSolver failure on springer fixed via `LD_LIBRARY_PATH` (not a GPU model issue —
  conda CUDA 12 libs needed to be explicitly added to path).
- Benchmark `solve_rate/*` eval added to `train.py` to match maze_plr.py WandB dashboard.

## Commits
- `2cd26e2`: fix(05.1-02): support checkpoint_final.pkl in vae_decoder + update config
- `d0c335a`: feat(05.1-02): add benchmark solve_rate eval to train.py + fix LD_LIBRARY_PATH
