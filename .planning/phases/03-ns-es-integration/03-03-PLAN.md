---
phase: 03-ns-es-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - tests/test_phase3_ns_es.py
autonomous: true
requirements:
  - ALGO-01
  - INTEG-01
  - INTEG-02
  - INTEG-03

must_haves:
  truths:
    - "tests/test_phase3_ns_es.py exists and all 6 tests pass under jax_env python"
    - "test_nses_strategy_ask_tell proves NSESStrategy ask/tell produces correct shapes and types"
    - "test_nses_tell_uses_composite_fitness proves tell() novelty score varies with actual novelty"
    - "test_behavior_sig_extraction proves extract_behavior_signature returns (pop_size, 169) L1-normalized float32"
    - "test_two_bucket_empty_buffer_guard proves empty buffer falls back to frontier (sample_replay_decision returns False)"
    - "test_archive_warmup_populates_buffer proves run_archive_warmup() inserts entries into sampler['size'] > 0"
    - "test_end_to_end_3_updates proves train.py runs 3 updates with warmup_n=4 and ns_es strategy without crashing"
  artifacts:
    - path: "tests/test_phase3_ns_es.py"
      provides: "6 integration/unit tests covering ALGO-01, INTEG-01, INTEG-02, INTEG-03"
      exports: []
      contains: "test_nses_strategy_ask_tell"
  key_links:
    - from: "tests/test_phase3_ns_es.py"
      to: "accel_training.es_components.nses_strategy"
      via: "NSESStrategy direct import"
      pattern: "NSESStrategy"
    - from: "tests/test_phase3_ns_es.py"
      to: "es.regret_fitness"
      via: "extract_behavior_signature import"
      pattern: "extract_behavior_signature"
    - from: "tests/test_phase3_ns_es.py"
      to: "accel_training.train.run_archive_warmup"
      via: "direct import and call with warmup_n=4"
      pattern: "run_archive_warmup"
---

<objective>
Write Phase 3 tests covering NSESStrategy, behavior_sig extraction, two-bucket sampling guard, archive warm-up buffer population, and a 3-update smoke end-to-end run.

Purpose: Tests prove Phase 3 delivers on all four requirements (ALGO-01, INTEG-01, INTEG-02, INTEG-03). Follow the same pattern as tests/test_es_components.py from Phase 2: standalone assert-based tests, no pytest dependency, run with jax_env python directly. Test 6 specifically exercises run_archive_warmup() to confirm it actually calls the function and populates the buffer — this was previously untested.

Output: tests/test_phase3_ns_es.py with 6 passing tests.
</objective>

<execution_context>
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/workflows/execute-plan.md
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ns-es-integration/03-CONTEXT.md
@.planning/phases/03-ns-es-integration/03-RESEARCH.md
@.planning/phases/03-ns-es-integration/03-01-SUMMARY.md
@.planning/phases/03-ns-es-integration/03-02-SUMMARY.md
@tests/test_es_components.py

<interfaces>
From tests/test_es_components.py (copy this pattern exactly):

Path setup block:
  import sys, os
  _HERE = os.path.dirname(os.path.abspath(__file__))
  _ROOT = os.path.dirname(_HERE)
  sys.path.insert(0, os.path.join(_ROOT, 'accel_training'))
  sys.path.insert(0, _ROOT)
  sys.path.insert(0, os.path.join(_ROOT, 'es'))

Test structure:
  def test_something():
      # assertions
      print("  PASS test_something")

  if __name__ == "__main__":
      print("Running Phase 3 NS-ES tests...")
      test_one()
      test_two()
      print("\nAll Phase 3 tests passed.")

NSESStrategy.tell() returns tuple (new_state_dict, mean_novelty_float).
extract_behavior_signature(agent_positions, num_steps) returns (pop_size, 169) float32 L1-normalized.
LevelSampler.sample_replay_decision returns False when buffer below minimum_fill_ratio.
run_archive_warmup(rng, rng_np, train_state, level_sampler, eval_fn, eval_env, env_params, network, config)
  -> (rng, rng_np, train_state) with train_state.sampler["size"] > 0 when warmup_n > 0.
train() returns (train_state, archive) — no exception = smoke test passes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/test_phase3_ns_es.py with 6 passing tests</name>
  <files>tests/test_phase3_ns_es.py</files>
  <action>
Create tests/test_phase3_ns_es.py. Follow tests/test_es_components.py structure exactly.

Set os.environ["WANDB_MODE"] = "disabled" at the very top of the file (before any imports) to prevent wandb network calls.

TEST 1 — test_nses_strategy_ask_tell (ALGO-01):
- NSESStrategy(param_dim=4, pop_size=3)
- init_state(rng, {"sigma_init": 0.5})
- ask(state, rng) — assert candidates.shape == (3, 4)
- tell(state, candidates, regrets=(3,), candidate_sigs=(3,4), buffer_sigs=(10,4), valid_mask=(10,) with 3 True, alpha=0.8, beta=0.2)
- assert return is tuple of length 2
- assert "es_state" in returned new_state
- assert isinstance(mean_novelty, float)
- Call tell() a second time (verify state updates cleanly)

TEST 2 — test_nses_tell_uses_composite_fitness (ALGO-01):
- NSESStrategy(param_dim=4, pop_size=2)
- init_state and ask once
- Scenario A: buffer_sigs = jnp.zeros((5, 4)), candidate_sigs = jnp.ones((2, 4)) — candidate far from buffer -> high novelty
- Scenario B: buffer_sigs = jnp.ones((5, 4)), candidate_sigs = jnp.ones((2, 4)) — candidate identical to buffer -> low novelty
- Call tell() for both scenarios
- assert novelty_A > novelty_B by a non-trivial margin (assert novelty_A > novelty_B + 0.01)
- This proves novelty is actually computed and varies with actual distance

TEST 3 — test_behavior_sig_extraction (INTEG-02):
- Import extract_behavior_signature from regret_fitness
- Create dummy agent_positions: jnp.array([[[2, 3]], [[5, 7]]]) shape (2, 1, 2) (2 steps, 1 env, 2D coord)
- Call extract_behavior_signature(agent_positions, num_steps=2)
- assert output.shape == (1, 169)
- assert abs(float(jnp.sum(output[0])) - 1.0) < 1e-4 (L1-normalized to sum=1)
- assert output.dtype == jnp.float32

TEST 4 — test_two_bucket_empty_buffer_guard (INTEG-01):
- Import LevelSampler from jaxued.level_sampler
- Import Maze from jaxued.environments.maze; import AutoReplayWrapper from jaxued.wrappers
- Build a minimal placeholder level (reuse the pattern from train.py for pholder_level)
- LevelSampler(capacity=10, replay_prob=0.8, staleness_coeff=0.3, minimum_fill_ratio=0.5, prioritization="rank", prioritization_params={"temperature":0.3,"k":4}, duplicate_check=False)
- sampler = level_sampler.initialize(pholder_level, {"max_return": jnp.array(-jnp.inf), "latent": jnp.zeros(64), "behavior_sig": jnp.zeros(169)})
- result = level_sampler.sample_replay_decision(sampler, jax.random.PRNGKey(0))
- assert bool(result) == False, "Empty buffer must fall back to frontier (no replay)"
- This proves the two-bucket empty-buffer guard: frontier sampling is the fallback when buffer is below minimum_fill_ratio

TEST 5 — test_archive_warmup_populates_buffer (INTEG-02):
Purpose: directly call run_archive_warmup() and prove it populates the PLR buffer (sampler['size'] > 0).
This is the specific test for INTEG-02 warm-up that was previously missing.

- Guard at top: if not os.path.exists("vae/model/checkpoint_420000.pkl"): print SKIP and return
- Import run_archive_warmup from accel_training.train
- Import build_eval_fn (or equivalent) and the network constructor used in train.py — mirror the setup in train.py's train() function
- Build a minimal train_state and sampler with capacity=32 by following the same init pattern as train.py (use config with warmup_n=4, num_train_envs=4, eval_rollout_steps matching config.yml)
- Call: rng_out, rng_np_out, train_state_out = run_archive_warmup(rng, rng_np, train_state, level_sampler, eval_fn, eval_env, env_params, network, config)
- assert int(train_state_out.sampler["size"]) > 0, "run_archive_warmup did not populate the buffer"
- assert int(train_state_out.sampler["size"]) <= 4, "warmup_n=4 should insert at most 4 entries"
- print("  PASS test_archive_warmup_populates_buffer — buffer size:", int(train_state_out.sampler["size"]))

TEST 6 — test_end_to_end_3_updates (INTEG-03):
- Import train from accel_training.train
- Build minimal config dict with all required keys. Use defaults from config.yml, override:
    num_updates: 3
    warmup_n: 4          # warm-up IS enabled (4 latents) — verifies warm-up runs in the smoke test
    wandb_log_freq: 1
    log_dir: "/tmp/test_phase3_smoke"
    vae_checkpoint: "vae/model/checkpoint_420000.pkl"
    es_strategy: "ns_es"
    es_alpha: 0.8
    es_beta: 0.2
    es_pop_size: 16
    es_sigma_init: 0.5
    es_k_novelty: 5
    wandb_project: "test"
    seed: 0
    (include all other keys from config.yml: lr, gamma, gae_lambda, etc.)
- Call train_state, archive = train(config)
- assert train_state is not None
- assert "size" in train_state.sampler
- This test uses warmup_n=4 (not 0) so run_archive_warmup actually executes — proving end-to-end integration including warm-up

If test_end_to_end_3_updates fails due to missing vae_checkpoint at path "vae/model/checkpoint_420000.pkl": add a guard at the start of the test:
  import os
  if not os.path.exists("vae/model/checkpoint_420000.pkl"):
      print("  SKIP test_end_to_end_3_updates (VAE checkpoint not found at vae/model/checkpoint_420000.pkl)")
      return
This prevents CI failures when the checkpoint is not available in the test environment.

Run all 6 tests sequentially in __main__. If any test raises an exception, let it propagate (no try/except in __main__).
  </action>
  <verify>
    <automated>/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python /cs/student/project_msc/2025/csml/gmaralla/superintelligence/tests/test_phase3_ns_es.py 2>&amp;1 | tail -20</automated>
  </verify>
  <done>All 6 tests print PASS (or SKIP for checkpoint-dependent tests if VAE checkpoint absent). Final line prints "All Phase 3 tests passed." run_archive_warmup() is exercised by at least one non-skipped test.</done>
</task>

</tasks>

<verification>
Phase 3 complete when:
1. test_phase3_ns_es.py runs to completion under jax_env python with all PASS lines
2. The assert in train.py no longer fires at startup (behavior_sig is populated before insert_batch)
3. config.yml contains es_strategy: ns_es and all ES config keys
4. Running train.py with default config (ns_es strategy) does not crash on the assert
5. test_archive_warmup_populates_buffer confirms run_archive_warmup() inserts entries (sampler['size'] > 0)
</verification>

<success_criteria>
- /cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python tests/test_phase3_ns_es.py exits 0
- Output contains "All Phase 3 tests passed." as final line
- No FAIL lines in output
- test_archive_warmup_populates_buffer prints PASS (not SKIP) — it must not depend on VAE checkpoint
</success_criteria>

<output>
After completion, create .planning/phases/03-ns-es-integration/03-03-SUMMARY.md
</output>
