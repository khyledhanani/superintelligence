---
phase: 03-reproducibility-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - examples/launch_llm_injection.sh
  - examples/launch_accel_only_control.sh
  - scripts/compare_llm_results.py
autonomous: false
requirements: [EXPT-02, EXPT-03]

must_haves:
  truths:
    - "launch_llm_injection.sh runs ACCEL+LLM training with 3 seeds on GPU nodes with --llm_inject_start_step and --llm_inject_interval flags"
    - "launch_accel_only_control.sh runs ACCEL-only control with matching seeds, buffer sizes, and non-injection hyperparameters"
    - "Both launch scripts use WandB project JAXUED_LLM with group names accel-llm and accel-only respectively"
    - "compare_llm_results.py queries JAXUED_LLM WandB runs and prints mean+std solve rate plus LLM-specific metrics per group"
    - "An ablation can be run by changing only --llm_inject_start_step and --llm_inject_interval in the launch script"
  artifacts:
    - path: "examples/launch_llm_injection.sh"
      provides: "ACCEL+LLM injection launch script for 3 seeds on GPU nodes"
      contains: "llm_inject_start_step"
    - path: "examples/launch_accel_only_control.sh"
      provides: "ACCEL-only control launch script with matching seeds"
      contains: "JAXUED_LLM"
    - path: "scripts/compare_llm_results.py"
      provides: "WandB comparison table for accel-llm vs accel-only groups"
      contains: "wandb.Api"
  key_links:
    - from: "examples/launch_llm_injection.sh"
      to: "examples/maze_plr.py"
      via: "--use_llm --use_accel --llm_inject_start_step flags"
      pattern: "llm_inject_start_step"
    - from: "scripts/compare_llm_results.py"
      to: "wandb API"
      via: "wandb.Api().runs() with JAXUED_LLM project filter"
      pattern: "api\\.runs"
---

<objective>
Create comparison launch scripts and WandB analysis tooling for the ACCEL+LLM vs ACCEL-only experiment.

Purpose: Phase 3 success criteria require that `launch_llm_injection.sh` and `launch_accel_only_control.sh` exist with matching seeds and conditions, and that ablation is possible by changing only injection flags. The comparison script enables quick results analysis after runs complete.

Output: Two launch scripts in `examples/` and one comparison script in `scripts/`.
</objective>

<execution_context>
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/workflows/execute-plan.md
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reproducibility-infrastructure/03-CONTEXT.md
@.planning/phases/03-reproducibility-infrastructure/03-RESEARCH.md

<interfaces>
<!-- Launch script pattern to follow exactly. From examples/launch_50k_pca_refit_accel.sh -->
```bash
#!/bin/bash
set -e
export LD_LIBRARY_PATH=/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/lib:${LD_LIBRARY_PATH:-}
PYTHON=/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python
COMMON="--project JAXUED_50K --num_updates 50000 --eval_freq 250 --skip_video --skip_post_eval"
export WANDB_DIR=/tmp/wandb
export JAX_COMPILATION_CACHE_DIR=/tmp/jax_cache
mkdir -p /tmp/jax_cache logs
for seed in 0 1 2; do
  $PYTHON examples/maze_plr.py $COMMON --seed $seed --run_name "..." 2>&1 | tee logs/...seed${seed}.log
done
```

<!-- WandB API pattern from vae/compare_accel_vs_cmaes.py -->
```python
api = wandb.Api()
wandb_runs = api.runs("JAXUED_LLM")
for run in wandb_runs:
    run_name = run.config.get("run_name", run.group)
    history = run.history(keys=["num_updates", "solve_rate/mean", ...])
```

<!-- Key maze_plr.py flags for LLM injection (from 03-01 plan, after rename): -->
--use_llm --llm_provider <provider> --llm_config llm/config.yaml
--llm_inject_start_step 5000 --llm_inject_interval 3000
--llm_gate --llm_batch_size 25

<!-- Key maze_plr.py flags for ACCEL-only (no LLM): -->
--use_accel (no --use_llm flag)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create launch scripts for LLM injection and ACCEL-only control</name>
  <files>examples/launch_llm_injection.sh, examples/launch_accel_only_control.sh</files>
  <action>
**1. Create `examples/launch_llm_injection.sh`:**

Follow the exact pattern from `launch_50k_pca_refit_accel.sh`. Key differences:
- WandB project: `JAXUED_LLM` (not JAXUED_50K)
- WandB group / run_name: `accel-llm`
- Flags: `--use_accel --use_llm --llm_provider claude-code --llm_config llm/config.yaml`
- Injection flags: `--llm_inject_start_step 5000 --llm_inject_interval 3000 --llm_batch_size 25`
- Gate flags: `--llm_gate` (gate enabled by default, but explicit is clearer)
- Training: `--num_updates 50000 --eval_freq 250 --skip_video --skip_post_eval`
- 3 seeds (0, 1, 2) in a for-loop, sequential on one machine
- Log files: `logs/llm_injection_seed${seed}.log`

**CRITICAL:** The script runs directly on the target GPU machine (user SSHes to albacore/smew/canada and runs it). The script does NOT SSH to other machines. Per Pitfall 5 from RESEARCH.md.

Add a comment header explaining: "Run this script on a GPU node (albacore, smew, or canada). One seed per machine for parallel execution, or all 3 seeds sequentially on one machine."

Add a comment block at the top showing the ablation parameters the user can change:
```bash
# === Ablation parameters (change these for ablation experiments) ===
INJECT_START=5000       # --llm_inject_start_step
INJECT_INTERVAL=3000    # --llm_inject_interval
BATCH_SIZE=25           # --llm_batch_size
```

Use these variables in the $PYTHON command so ablation only requires changing one place.

**2. Create `examples/launch_accel_only_control.sh`:**

Same pattern but WITHOUT LLM flags:
- WandB project: `JAXUED_LLM` (same project for comparison!)
- WandB group / run_name: `accel-only`
- Flags: `--use_accel` (NO `--use_llm`)
- Same training hyperparameters: `--num_updates 50000 --eval_freq 250 --skip_video --skip_post_eval`
- Same 3 seeds (0, 1, 2) for matching
- Log files: `logs/accel_only_control_seed${seed}.log`

Comment header: "Run this script on a GPU node or TPU. Control condition for ACCEL+LLM comparison."

**Both scripts MUST include:**
- `set -e`
- `export LD_LIBRARY_PATH=...` (CUDA 13.1 fix from MEMORY.md)
- `export WANDB_DIR=/tmp/wandb`
- `export JAX_COMPILATION_CACHE_DIR=/tmp/jax_cache`
- `mkdir -p /tmp/jax_cache logs`
- The same `--num_updates`, `--eval_freq`, `--skip_video`, `--skip_post_eval` values
- `chmod +x` both files after creation

**Matching conditions:** The non-injection hyperparameters MUST be identical between the two scripts. Define a `COMMON` variable with shared flags to make this explicit.
  </action>
  <verify>
    <automated>cd /cs/student/project_msc/2025/csml/gmaralla/superintelligence && bash -c "
set -e
# Check both scripts exist and are executable
test -f examples/launch_llm_injection.sh && echo 'LLM script exists'
test -f examples/launch_accel_only_control.sh && echo 'Control script exists'
test -x examples/launch_llm_injection.sh && echo 'LLM script executable'
test -x examples/launch_accel_only_control.sh && echo 'Control script executable'

# Check LLM script has required flags
grep -q 'llm_inject_start_step' examples/launch_llm_injection.sh && echo 'LLM: has inject_start_step'
grep -q 'llm_inject_interval' examples/launch_llm_injection.sh && echo 'LLM: has inject_interval'
grep -q 'JAXUED_LLM' examples/launch_llm_injection.sh && echo 'LLM: has JAXUED_LLM project'
grep -q 'accel-llm' examples/launch_llm_injection.sh && echo 'LLM: has accel-llm group'
grep -q 'use_llm' examples/launch_llm_injection.sh && echo 'LLM: has --use_llm'
grep -q 'use_accel' examples/launch_llm_injection.sh && echo 'LLM: has --use_accel'
grep -q 'LD_LIBRARY_PATH' examples/launch_llm_injection.sh && echo 'LLM: has LD_LIBRARY_PATH fix'

# Check control script has matching conditions but no LLM flags
grep -q 'JAXUED_LLM' examples/launch_accel_only_control.sh && echo 'Control: has JAXUED_LLM project'
grep -q 'accel-only' examples/launch_accel_only_control.sh && echo 'Control: has accel-only group'
grep -q 'use_accel' examples/launch_accel_only_control.sh && echo 'Control: has --use_accel'
! grep -q 'use_llm' examples/launch_accel_only_control.sh && echo 'Control: no --use_llm (correct)'
grep -q 'LD_LIBRARY_PATH' examples/launch_accel_only_control.sh && echo 'Control: has LD_LIBRARY_PATH fix'

# Check matching seeds
grep -q 'for seed in 0 1 2' examples/launch_llm_injection.sh && echo 'LLM: seeds 0 1 2'
grep -q 'for seed in 0 1 2' examples/launch_accel_only_control.sh && echo 'Control: seeds 0 1 2'

echo 'ALL LAUNCH SCRIPT CHECKS PASSED'
"
</automated>
  </verify>
  <done>
- `examples/launch_llm_injection.sh` exists with --use_llm, --use_accel, --llm_inject_start_step, --llm_inject_interval, JAXUED_LLM project, accel-llm group, 3 seeds
- `examples/launch_accel_only_control.sh` exists with --use_accel only, matching training hyperparameters, JAXUED_LLM project, accel-only group, 3 seeds
- Both scripts have LD_LIBRARY_PATH fix, WANDB_DIR, JAX_COMPILATION_CACHE_DIR
- Ablation parameters are extracted as variables at the top of the LLM script
  </done>
</task>

<task type="auto">
  <name>Task 2: Create WandB comparison script</name>
  <files>scripts/compare_llm_results.py</files>
  <action>
Create `scripts/compare_llm_results.py` that queries WandB API and prints a comparison table.

**Structure:**
```python
#!/usr/bin/env python3
"""Compare ACCEL+LLM injection vs ACCEL-only control runs from JAXUED_LLM WandB project."""
import argparse
import numpy as np
import wandb
```

**Logic:**
1. Parse optional `--project` (default `JAXUED_LLM`) and `--entity` (default None) args
2. `api = wandb.Api()` → `runs = api.runs(f"{entity}/{project}" if entity else project)`
3. Group runs by `run.config.get("run_name")` — matching `accel-llm` and `accel-only`
4. For each group, collect per-run final metrics:
   - `solve_rate/mean`: final value from `run.history(keys=["solve_rate/mean"])`
   - For `accel-llm` group also collect: `llm/acceptance_rate`, `llm/injected_count`, `llm/diversity_score_mean` (final values)
5. Print a formatted table:
```
=== JAXUED_LLM Comparison ===

Group           | N | Solve Rate (mean ± std) | Acceptance Rate | Injected Count | Diversity Score
accel-llm       | 3 | 0.742 ± 0.031          | 0.65 ± 0.08    | 125.3 ± 12.1   | 0.034 ± 0.005
accel-only      | 3 | 0.685 ± 0.045          | -               | -               | -
```
6. Print per-run detail table (run name, seed, final solve rate, state)

**Follow the `wandb.Api()` pattern from `vae/compare_accel_vs_cmaes.py`** but simplified (no matplotlib, no GCS uploads).

Handle edge cases:
- Empty runs (no history) → skip with warning
- Missing metrics → show "N/A"
- No runs found for a group → print "No runs found"

Add `if __name__ == "__main__":` guard.
  </action>
  <verify>
    <automated>cd /cs/student/project_msc/2025/csml/gmaralla/superintelligence && /cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python -c "
# Verify script syntax and imports
import ast
with open('scripts/compare_llm_results.py') as f:
    source = f.read()
ast.parse(source)
print('Syntax: VALID')

# Check key patterns exist
assert 'wandb.Api' in source, 'Missing wandb.Api'
assert 'JAXUED_LLM' in source, 'Missing JAXUED_LLM default'
assert 'accel-llm' in source, 'Missing accel-llm group'
assert 'accel-only' in source, 'Missing accel-only group'
assert 'solve_rate' in source, 'Missing solve_rate metric'
assert 'acceptance_rate' in source or 'llm/acceptance_rate' in source, 'Missing acceptance_rate'
assert 'argparse' in source, 'Missing argparse'
assert '__main__' in source, 'Missing __main__ guard'
print('Content checks: ALL PASSED')
"
</automated>
  </verify>
  <done>
- `scripts/compare_llm_results.py` exists with valid Python syntax
- Queries JAXUED_LLM WandB project using wandb.Api()
- Groups by run_name (accel-llm, accel-only)
- Prints mean+std solve rate, acceptance rate, injected count, diversity score per group
- Has --project and --entity CLI args for flexibility
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify launch scripts and comparison tooling</name>
  <files>examples/launch_llm_injection.sh, examples/launch_accel_only_control.sh, scripts/compare_llm_results.py</files>
  <action>
User verifies that launch scripts and comparison tooling match their experimental setup. No code changes — this is a human review checkpoint.

What was built: Two launch scripts (ACCEL+LLM and ACCEL-only control) with matching experimental conditions, and a WandB comparison script. The LLM script has ablation parameters at the top that can be changed independently.

How to verify:
1. Open `examples/launch_llm_injection.sh` and confirm:
   - Uses `--project JAXUED_LLM` and `--run_name accel-llm`
   - Has `--use_llm --use_accel` flags
   - Ablation variables (INJECT_START, INJECT_INTERVAL) at top
   - LD_LIBRARY_PATH CUDA fix present
   - 3 seeds in for-loop

2. Open `examples/launch_accel_only_control.sh` and confirm:
   - Uses `--project JAXUED_LLM` and `--run_name accel-only`
   - Has `--use_accel` but NO `--use_llm`
   - Same training hyperparameters as LLM script (num_updates, eval_freq)

3. Run `python scripts/compare_llm_results.py --help` to verify CLI works

4. Type "approved" if scripts look correct for your experiment setup, or describe issues.
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>User confirms scripts match their experimental setup — launch scripts ready for Phase 4 execution</done>
</task>

</tasks>

<verification>
1. Both launch scripts exist, are executable, and have matching non-injection hyperparameters
2. LLM script has `--llm_inject_start_step` and `--llm_inject_interval` as ablation-friendly variables
3. Control script has NO LLM flags
4. Both scripts target `JAXUED_LLM` WandB project
5. Comparison script has valid syntax, queries WandB, and groups by accel-llm/accel-only
6. User verifies scripts match their experimental setup (checkpoint)
</verification>

<success_criteria>
- Phase 3 success criteria 3: `launch_llm_injection.sh` and `launch_accel_only_control.sh` exist with matching seeds and buffer sizes
- Phase 3 success criteria 4: `--llm_inject_start_step` and `--llm_inject_interval` are independently configurable for ablation
- EXPT-02 audit trail complete: disk cache (03-01) + WandB hashes (03-01) + comparison tooling (this plan)
- EXPT-03 ablation: launch script with parameterized injection flags enables ablation by changing only those values
</success_criteria>

<output>
After completion, create `.planning/phases/03-reproducibility-infrastructure/03-02-SUMMARY.md`
</output>
