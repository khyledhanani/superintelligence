---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation/AGENT_VERIFICATION.md
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "AGENT_VERIFICATION.md exists and documents every found difference between our PPO/ACCEL implementation and the DCD repo defaults"
    - "A smoke test of maze_plr.py with --use_accel --score_function MaxMC completes 5 updates without crashing"
    - "The smoke test logs show regret > 0.0 and changing across updates, and solve rate between 0.0 and 1.0"
  artifacts:
    - path: ".planning/phases/01-foundation/AGENT_VERIFICATION.md"
      provides: "Flat list of all PPO/ACCEL implementation differences vs DCD, plus smoke test results"
      contains: "gae_lambda, entropy_coeff, score_function, level_sampling"
  key_links:
    - from: "examples/maze_plr.py compute_gae()"
      to: "DCD gae_lambda=0.95 vs ours=0.98"
      via: "manual code inspection, documented in AGENT_VERIFICATION.md"
      pattern: "gae_lambda"
    - from: "smoke test stdout/wandb offline logs"
      to: "AGENT_VERIFICATION.md smoke test section"
      via: "run output captured and appended"
      pattern: "regret.*[0-9]"
---

<objective>
Verify that our PPO/ACCEL agent implementation is a legitimate baseline by (a) comparing its code against DCD repo defaults and (b) running a short smoke test confirming the pipeline is functional.

Purpose: FOUND-01 — establish that the agent we are building ES around is a valid ACCEL implementation, with all differences from DCD explicitly documented.
Output: `.planning/phases/01-foundation/AGENT_VERIFICATION.md` — a flat list of all differences (intentional or potential bug) plus smoke test run results.
</objective>

<execution_context>
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/workflows/execute-plan.md
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key facts from codebase inspection: -->

From examples/maze_plr.py (argparse defaults):
- lr = 1e-4
- gamma = 0.995
- gae_lambda = 0.98         (DCD default: 0.95 — DIFFERENCE)
- clip_eps = 0.2
- entropy_coeff = 1e-3      (DCD default: 0.0 — DIFFERENCE)
- critic_coeff = 0.5
- max_grad_norm = 0.5
- epoch_ppo = 5
- num_minibatches = 1
- num_steps = 256
- num_train_envs = 32
- score_function = MaxMC    (DCD generic default: value_l1/PVL — BUT ACCEL configs use MaxMC; matches ACCEL config)

From es/regret_fitness.py:
- Uses compute_max_returns + max_mc from jaxued.utils (jaxued = JAX reimplementation of DCD)
- rollout_agent_on_levels returns (rewards, values, dones)
- AutoReplayWrapper: agent replays same level after episode end

Key structural difference — level sampling:
- DCD ACCEL: PLR buffer + minimax random mutation
- Our code: PLR buffer + MAP-Elites / ES latent-space mutation
- Verdict: INTENTIONAL architectural difference (the thesis contribution)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PPO Code Comparison — Write AGENT_VERIFICATION.md</name>
  <files>.planning/phases/01-foundation/AGENT_VERIFICATION.md</files>
  <action>
Read `examples/maze_plr.py` fully, focusing on:
1. `compute_gae()` — confirm GAE formula: delta = r + gamma * V(s') * (1-d) - V(s), gae = delta + gamma * lambda * (1-d) * gae
2. The PPO update loop — clipped surrogate objective, value loss clipping (clip_eps applied to value loss), entropy bonus with entropy_coeff
3. All argparse defaults (lr, gamma, gae_lambda, clip_eps, entropy_coeff, critic_coeff, max_grad_norm, epoch_ppo, num_minibatches, num_steps, num_train_envs, score_function)
4. The regret computation path: how compute_max_returns and max_mc are called
5. Level sampling method (minimax mutator vs ES/MAP-Elites)

Also read `es/regret_fitness.py` for `max_mc` usage.

Then create `.planning/phases/01-foundation/AGENT_VERIFICATION.md` with the following structure:

```markdown
# Agent Verification Report

**Phase:** 01-Foundation
**Date:** [today]
**Compared against:** DCD repo (github.com/facebookresearch/dcd) arguments.py defaults

## PPO Hyperparameter Comparison

| Parameter | DCD default | Our default | Match? | Classification |
|-----------|-------------|-------------|--------|----------------|
| lr | 1e-4 | [actual] | [YES/NO] | [intentional/potential-bug/matches] |
| gamma | 0.995 | [actual] | ... | ... |
| gae_lambda | 0.95 | [actual] | ... | ... |
| clip_eps | 0.2 | [actual] | ... | ... |
| entropy_coeff | 0.0 | [actual] | ... | ... |
| critic_coeff | 0.5 | [actual] | ... | ... |
| max_grad_norm | 0.5 | [actual] | ... | ... |
| epoch_ppo | 5 | [actual] | ... | ... |
| num_minibatches | 1 | [actual] | ... | ... |
| num_steps | 256 | [actual] | ... | ... |
| num_train_envs | 32 | [actual] | ... | ... |
| score_function | value_l1 (generic) / MaxMC (ACCEL) | [actual] | ... | ... |

## PPO Algorithm Comparison

### GAE (compute_gae)
[Document the GAE formula as found in maze_plr.py vs DCD formula. Note any differences.]

### Update Epoch (update_epoch / update_step)
[Document clipped surrogate, value loss, entropy bonus. Note any differences.]

### Regret Computation
[Document how max_mc is called. Note: jaxued.utils.max_mc is the JAX reimplementation of DCD's MaxMC.]

## Structural Differences

### Level Sampling
- DCD ACCEL: PLR buffer + minimax random mutation (make_level_mutator_minimax)
- Our implementation: PLR buffer + MAP-Elites / ES latent-space mutation
- Classification: INTENTIONAL — this is the thesis contribution (ES-generated curriculum)

[List any other structural differences found]

## Summary of Differences

[Flat list of ALL differences, one per line, classified as:
- INTENTIONAL: known design choice
- POTENTIAL-BUG: warrants further investigation
- MATCHES-ACCEL-CONFIG: differs from DCD generic default but matches DCD ACCEL-specific config]

## Smoke Test Results

[To be filled in by Task 2]
```

Fill in every table row and section with the actual values found in the code. Do not leave placeholders — inspect the code and record what is actually there. If a parameter is not in argparse, note where it is set.
  </action>
  <verify>
    <automated>test -f /cs/student/project_msc/2025/csml/gmaralla/superintelligence/.planning/phases/01-foundation/AGENT_VERIFICATION.md && grep -c "gae_lambda" /cs/student/project_msc/2025/csml/gmaralla/superintelligence/.planning/phases/01-foundation/AGENT_VERIFICATION.md</automated>
  </verify>
  <done>AGENT_VERIFICATION.md exists, contains the hyperparameter comparison table with actual values filled in (not placeholders), and has a "Structural Differences" section documenting the level sampling difference as intentional.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke Test — Run ACCEL for 5 Updates and Append Results</name>
  <files>.planning/phases/01-foundation/AGENT_VERIFICATION.md</files>
  <action>
Run the smoke test from the project root directory using the exact command below. Capture stdout/stderr to a temp file, then append results to AGENT_VERIFICATION.md.

Command:
```bash
cd /cs/student/project_msc/2025/csml/gmaralla/superintelligence

WANDB_MODE=offline python examples/maze_plr.py \
  --use_accel \
  --score_function MaxMC \
  --num_updates 5 \
  --num_train_envs 32 \
  --num_steps 256 \
  --seed 42 \
  --project FOUNDATION_SMOKE_TEST \
  --run_name smoke_test_accel_maxmc \
  --checkpoint_save_interval 999 \
  2>&1 | tee /tmp/smoke_test_output.txt
```

After the run completes (or fails), append a "Smoke Test Results" section to AGENT_VERIFICATION.md documenting:

1. **Exit status:** Did the script exit cleanly (exit code 0)?
2. **Crash check:** Any Python exceptions or JAX errors in output? Paste the last 30 lines of output.
3. **Regret check:** Extract regret values logged per update. Are they > 0.0 and changing across updates? (grep for "regret" in output)
4. **Solve rate check:** Is eval solve rate between 0.0 and 1.0? (grep for "solved_rate" or "eval" in output)
5. **WandB offline:** Did offline logging succeed without network errors?

Passing bar (per CONTEXT.md):
- No crash
- Regret > 0 and changing across updates
- Solve rate between 0.0 and 1.0
- Script exits cleanly after 5 updates

If the smoke test fails (crash, OOM, etc.), document the failure in AGENT_VERIFICATION.md with the full error message and the last 50 lines of output. Do NOT mark the task as failed — document what happened and continue.

Note on duration: `--num_updates 5` processes 5 gradient updates, each over 32 envs * 256 steps = 8192 environment steps. Total ~5 minutes on GPU or ~20 minutes on CPU. If running on CPU, expect this to take longer.

If running without GPU acceleration and the test takes too long, reduce to `--num_updates 2` and document this in the report.
  </action>
  <verify>
    <automated>grep -i "Smoke Test Results" /cs/student/project_msc/2025/csml/gmaralla/superintelligence/.planning/phases/01-foundation/AGENT_VERIFICATION.md && grep -i "exit\|crash\|regret\|pass\|fail" /cs/student/project_msc/2025/csml/gmaralla/superintelligence/.planning/phases/01-foundation/AGENT_VERIFICATION.md | head -10</automated>
  </verify>
  <done>AGENT_VERIFICATION.md contains a "Smoke Test Results" section with: exit status, crash check, regret values observed, solve rate observed, and a PASS or FAIL verdict with justification. The section is filled with actual run data, not placeholders.</done>
</task>

</tasks>

<verification>
1. `.planning/phases/01-foundation/AGENT_VERIFICATION.md` exists and is non-empty
2. Hyperparameter table has actual values (not "[actual]" placeholders) for all 12 parameters
3. gae_lambda and entropy_coeff differences are documented
4. Level sampling structural difference is classified as INTENTIONAL
5. Smoke test results section exists with actual output data
6. If smoke test passed: regret was non-zero and changing, solve rate was in (0, 1), no crash
</verification>

<success_criteria>
AGENT_VERIFICATION.md documents all PPO/ACCEL differences vs DCD defaults in a flat list. A smoke test run of maze_plr.py with ACCEL+MaxMC either (a) passes — no crash, regret > 0 and changing, solve rate in (0,1) — or (b) fails with the exact error documented for debugging. Either way, FOUND-01 is satisfied: we know the state of the baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation-01-SUMMARY.md` with:
- What was verified (hyperparameter diffs, structural diffs)
- Smoke test result (PASS/FAIL + key metrics observed)
- Files created
- Any concerns or follow-up items
</output>
