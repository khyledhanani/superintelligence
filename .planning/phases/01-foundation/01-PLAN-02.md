---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - es/regret_fitness.py
  - .planning/DECISIONS.md
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "extract_behavior_signature() exists in es/regret_fitness.py and returns a (pop_size, 169) float32 JAX array"
    - "rollout_agent_on_levels_with_positions exists and returns agent_pos with shape (num_steps, pop_size, 2)"
    - "jax.jit(extract_behavior_signature).lower(dummy_args).compile() succeeds without error"
    - "Behavior signatures for a sparse maze and a dense maze are visually distinct (confirmed by inspection)"
    - "DECISIONS.md exists with DECISION-01 documenting the v1 design rationale and planned revisit criteria"
    - "The function is marked with the EXPERIMENTAL v1 comment referencing .planning/DECISIONS.md"
  artifacts:
    - path: "es/regret_fitness.py"
      provides: "extract_behavior_signature() function plus rollout wrapper to emit agent_pos"
      exports: ["extract_behavior_signature", "rollout_agent_on_levels_with_positions"]
      contains: "TODO: EXPERIMENTAL v1"
    - path: ".planning/DECISIONS.md"
      provides: "DECISION-01 behavior signature design log"
      contains: "DECISION-01"
  key_links:
    - from: "es/regret_fitness.py rollout_agent_on_levels"
      to: "extract_behavior_signature agent_positions arg"
      via: "rollout_agent_on_levels_with_positions wrapper emitting agent_pos from step_fn"
      pattern: "agent_pos"
    - from: "extract_behavior_signature"
      to: "jax.nn.one_hot(cell_idx, num_classes=169).sum(axis=0)"
      via: "one-hot histogram pattern (JIT-safe; no dynamic shapes)"
      pattern: "one_hot"
---

<objective>
Implement extract_behavior_signature() in es/regret_fitness.py — a JAX-JIT-compatible function that takes an agent's position trajectory from a rollout and returns a fixed-length L1-normalized visit-count histogram over the 13x13 maze grid.

Purpose: FOUND-02 — the behavior signature is the core primitive that enables all subsequent ES diversity mechanisms (NS-ES, SV-CMA-ES). Everything in Phases 2-4 depends on this function existing, being JIT-compatible, and producing distinct signatures for qualitatively different levels.
Output: Extended es/regret_fitness.py with new functions plus .planning/DECISIONS.md documenting the design choice.
</objective>

<execution_context>
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/workflows/execute-plan.md
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key contracts from es/regret_fitness.py that must be preserved and extended: -->

Current rollout_agent_on_levels signature (MUST NOT CHANGE -- callers depend on it):
  def rollout_agent_on_levels(rng, env, env_params, agent_params, network, levels,
                              num_steps=256, deterministic=True):
      Returns: rewards (num_steps, pop_size), values (num_steps, pop_size), dones (num_steps, pop_size)

Current step_fn return (inside rollout_agent_on_levels):
  return (rng, hstate, next_obs, next_state, next_done), (reward, value, next_done)

EnvState.agent_pos convention (from env_bridge.py and jaxued maze env):
  agent_pos[..., 0] = col (x)  -- DO NOT confuse with row
  agent_pos[..., 1] = row (y)
  Linear index = row * grid_width + col = agent_pos[..., 1] * 13 + agent_pos[..., 0]

JIT-safe pattern (use exactly this, not alternatives):
  cell_idx = agent_positions[..., 1] * grid_w + agent_positions[..., 0]  # row*W + col
  one_hot = jax.nn.one_hot(cell_idx, num_classes=num_cells, dtype=jnp.float32)
  hist = one_hot.sum(axis=0)  # (pop_size, num_cells)
  total = hist.sum(axis=-1, keepdims=True)
  hist = hist / jnp.maximum(total, 1.0)

DO NOT USE (JIT-incompatible):
  - jnp.bincount (dynamic shape)
  - jnp.unique (dynamic shape)
  - Python for loops over steps
  - jnp.zeros(...).at[cell_idx].add(1) with multi-dimensional dynamic indices
  - Collecting full EnvState in scan (use only next_state.agent_pos to avoid OOM from maze_map)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend es/regret_fitness.py with rollout wrapper and extract_behavior_signature</name>
  <files>es/regret_fitness.py</files>
  <action>
Read es/regret_fitness.py fully. Then append two new functions after the existing rollout_agent_on_levels function. Do NOT modify the existing rollout_agent_on_levels function signature or return value -- callers in map_elites.py, evolve_envs.py depend on it.

Addition 1 -- rollout_agent_on_levels_with_positions:

This is a thin wrapper that duplicates rollout_agent_on_levels but modifies the step_fn to also emit next_state.agent_pos (shape: pop_size, 2). Emit ONLY agent_pos, NOT the full EnvState (avoid OOM from maze_map which is (pop_size, padded_H, padded_W, 3)).

The function should have this docstring and signature:
  def rollout_agent_on_levels_with_positions(rng, env, env_params, agent_params, network, levels,
                                              num_steps=256, deterministic=True):
  Returns: rewards (num_steps, pop_size), values (num_steps, pop_size),
           dones (num_steps, pop_size), agent_positions (num_steps, pop_size, 2)
  Docstring: "Thin wrapper around rollout_agent_on_levels that also emits agent_pos from the scan. Backward-compatible: existing callers of rollout_agent_on_levels are unaffected."

The step_fn inside this wrapper changes the emit from:
  return (rng, hstate, next_obs, next_state, next_done), (reward, value, next_done)
to:
  return (rng, hstate, next_obs, next_state, next_done), (reward, value, next_done, next_state.agent_pos)

Addition 2 -- extract_behavior_signature:

Add after rollout_agent_on_levels_with_positions. Use the one-hot-sum pattern exactly. The function must include:
  - The EXPERIMENTAL v1 TODO comment: "# TODO: EXPERIMENTAL v1 -- behavior signature design is NOT final. See .planning/DECISIONS.md for design rationale and planned revisit criteria."
  - Full docstring with Args, Returns, and Usage example showing jax.jit(...).lower(...).compile()
  - Coordinate convention note: agent_positions[..., 0]=col, agent_positions[..., 1]=row

Signature: extract_behavior_signature(agent_positions, num_steps, grid_h=13, grid_w=13)
Returns: (pop_size, grid_h * grid_w) float32, L1-normalized

Implementation body:
  num_cells = grid_h * grid_w  # 169
  col = agent_positions[..., 0].astype(jnp.int32)
  row = agent_positions[..., 1].astype(jnp.int32)
  cell_idx = row * grid_w + col
  one_hot = jax.nn.one_hot(cell_idx, num_classes=num_cells, dtype=jnp.float32)
  hist = one_hot.sum(axis=0)  # (pop_size, num_cells)
  total = hist.sum(axis=-1, keepdims=True)
  hist = hist / jnp.maximum(total, 1.0)
  return hist

After writing, run a syntax check:
  cd /cs/student/project_msc/2025/csml/gmaralla/superintelligence
  python -c "import sys; sys.path.insert(0, 'es'); import regret_fitness; print('Parse OK')"
  </action>
  <verify>
    <automated>cd /cs/student/project_msc/2025/csml/gmaralla/superintelligence && python -c "import sys; sys.path.insert(0, 'es'); import regret_fitness; assert hasattr(regret_fitness, 'extract_behavior_signature'); assert hasattr(regret_fitness, 'rollout_agent_on_levels_with_positions'); print('PASS: both functions exist')"</automated>
  </verify>
  <done>es/regret_fitness.py parses without error, contains extract_behavior_signature and rollout_agent_on_levels_with_positions, and the original rollout_agent_on_levels signature is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: JIT compatibility test, visual distinctness check, and create DECISIONS.md</name>
  <files>.planning/DECISIONS.md</files>
  <action>
Run three verification steps, then create DECISIONS.md.

Step A -- JIT compilation test:
Run the following Python script to verify the extractor compiles cleanly under jax.jit:

  cd /cs/student/project_msc/2025/csml/gmaralla/superintelligence
  python - <<'PYEOF'
  import jax
  import jax.numpy as jnp
  import sys
  sys.path.insert(0, 'es')
  from regret_fitness import extract_behavior_signature

  # Create dummy agent_positions with static shape (num_steps=4, pop_size=2, 2)
  dummy_positions = jnp.zeros((4, 2, 2), dtype=jnp.int32)

  # Test 1: eager call
  sig = extract_behavior_signature(dummy_positions, num_steps=4)
  assert sig.shape == (2, 169), f"Expected (2, 169), got {sig.shape}"
  assert sig.dtype == jnp.float32, f"Expected float32, got {sig.dtype}"
  print(f"Eager call: shape={sig.shape}, dtype={sig.dtype} -- PASS")

  # Test 2: jit.lower().compile() -- the required JIT compatibility test
  lowered = jax.jit(extract_behavior_signature).lower(dummy_positions, 4)
  lowered.compile()
  print("jit.lower().compile(): PASS")
  PYEOF

If this fails, inspect the error and fix es/regret_fitness.py before continuing.

Step B -- visual distinctness check:
Run the following to generate two synthetic position trajectories (sparse vs dense coverage) and print their histograms side by side:

  cd /cs/student/project_msc/2025/csml/gmaralla/superintelligence
  python - <<'PYEOF'
  import jax
  import jax.numpy as jnp
  import sys
  sys.path.insert(0, 'es')
  from regret_fitness import extract_behavior_signature

  # Sparse maze: agent stays near top-left corner (cells 0-2 in both dims)
  sparse_positions = jnp.stack([
      jnp.zeros((256, 1), dtype=jnp.int32),  # col=0
      jnp.zeros((256, 1), dtype=jnp.int32),  # row=0
  ], axis=-1)  # (256, 1, 2)

  # Dense maze: agent visits all quadrants -- spread uniformly across 13x13
  import numpy as np
  rng = np.random.default_rng(42)
  dense_cols = jnp.array(rng.integers(0, 13, size=(256, 1)), dtype=jnp.int32)
  dense_rows = jnp.array(rng.integers(0, 13, size=(256, 1)), dtype=jnp.int32)
  dense_positions = jnp.stack([dense_cols, dense_rows], axis=-1)  # (256, 1, 2)

  sparse_sig = extract_behavior_signature(sparse_positions, num_steps=256)
  dense_sig  = extract_behavior_signature(dense_positions,  num_steps=256)

  print("Sparse maze signature (top 10 nonzero cells):")
  sparse_arr = sparse_sig[0]
  top_sparse = jnp.argsort(-sparse_arr)[:10]
  for idx in top_sparse:
      val = float(sparse_arr[idx])
      if val > 0:
          print(f"  cell {int(idx):3d} (row={int(idx)//13}, col={int(idx)%13}): {val:.4f}")

  print("\nDense maze signature (top 10 nonzero cells):")
  dense_arr = dense_sig[0]
  top_dense = jnp.argsort(-dense_arr)[:10]
  for idx in top_dense:
      val = float(dense_arr[idx])
      if val > 0:
          print(f"  cell {int(idx):3d} (row={int(idx)//13}, col={int(idx)%13}): {val:.4f}")

  # Compute L1 distance between signatures as distinctness measure
  l1_dist = float(jnp.sum(jnp.abs(sparse_sig - dense_sig)))
  print(f"\nL1 distance between signatures: {l1_dist:.4f} (expected > 0.5 for visually distinct)")
  assert l1_dist > 0.1, f"Signatures too similar: L1={l1_dist}"
  print("Visual distinctness: PASS")
  PYEOF

Step C -- create .planning/DECISIONS.md:
Create the file with DECISION-01 as the first entry. Use this exact structure:

---
# Key Architectural Decisions

This document tracks key architectural decisions for the ES-ACCEL integration project.
Collaborators: refer to this document before modifying any EXPERIMENTAL-marked code.

---

## DECISION-01: Behavior Signature v1 Design

**Date:** 2026-02-28
**Status:** EXPERIMENTAL -- subject to revision after NS-ES validation (Phase 3)
**Requirement:** FOUND-02
**Code location:** es/regret_fitness.py::extract_behavior_signature()
**Marked with:** # TODO: EXPERIMENTAL v1

### Decision

v1 behavior signature is a 169-element (13x13) L1-normalized visit-count histogram
over the CLUTTR maze grid. Each element represents the fraction of rollout steps the
agent spent in that grid cell.

### Rationale

- Visit counts encode "where did the agent spend time?" -- captures maze traversal
  pattern without committing to trajectory ordering or action semantics
- 13x13 = 169 cells matches the CLUTTR maze resolution (inner_dim=13) -- no lossy
  binning in v1; full spatial resolution preserved
- L1 normalization makes signatures comparable across rollout lengths
- One-hot-sum pattern (jax.nn.one_hot(...).sum(axis=0)) is JIT-safe and vmap-safe
  without scatter complications
- Multi-episode accumulation (accumulate ALL steps across episode boundaries) gives
  richer coverage signal than single-episode; AutoReplayWrapper replays same level

### Known Limitations of v1

- Ignores action sequence (circling vs. straight path look the same if cells match)
- Does not capture temporal ordering of visits (bag-of-positions, not trajectory)
- May conflate topologically different mazes that happen to produce similar paths
- If the agent rarely reaches far cells, many bins near-zero carry no signal

### Planned Revisit Criteria

- After Phase 3 NS-ES validation: check if regret curves improve vs. vanilla CMA-ES
- If novelty reward hacks (high novelty, flat regret): redesign signature
- If mode collapse persists despite NS-ES: consider action-sequence or temporal features
- Ablation in Phase 5: compare 13x13 vs 7x7 resolution if k-NN is slow in Phase 2

### Implementation Reference

From RESEARCH.md recommendation:
  Grid resolution: 13x13 = 169 cells (full resolution)
  Normalization: L1 (divide by total steps per row)
  Episode boundary: Ignore (accumulate all steps)
  Temporal order: Ignored (histogram = bag of visits)

---
*Document created: 2026-02-28*
*Next review: After Phase 3 NS-ES training runs*
---

After creating the file, confirm it exists:
  test -f /cs/student/project_msc/2025/csml/gmaralla/superintelligence/.planning/DECISIONS.md && echo "DECISIONS.md created"
  </action>
  <verify>
    <automated>cd /cs/student/project_msc/2025/csml/gmaralla/superintelligence && python -c "import sys; sys.path.insert(0, 'es'); import jax; import jax.numpy as jnp; from regret_fitness import extract_behavior_signature; dummy = jnp.zeros((4,2,2), dtype=jnp.int32); jax.jit(extract_behavior_signature).lower(dummy, 4).compile(); print('JIT PASS')" && test -f .planning/DECISIONS.md && grep -q "DECISION-01" .planning/DECISIONS.md && echo "DECISIONS.md PASS"</automated>
  </verify>
  <done>jax.jit(extract_behavior_signature).lower(dummy_positions, num_steps).compile() completes without error. Sparse and dense trajectory signatures have L1 distance greater than 0.1 (visually distinct). .planning/DECISIONS.md exists and contains DECISION-01 with design rationale and revisit criteria.</done>
</task>

</tasks>

<verification>
1. es/regret_fitness.py contains extract_behavior_signature (with EXPERIMENTAL v1 TODO comment)
2. es/regret_fitness.py contains rollout_agent_on_levels_with_positions
3. Original rollout_agent_on_levels signature is unchanged
4. jax.jit(extract_behavior_signature).lower(dummy_args).compile() passes
5. extract_behavior_signature returns shape (pop_size, 169), dtype float32
6. Sparse maze signature and dense maze signature have L1 distance greater than 0.1
7. .planning/DECISIONS.md exists with DECISION-01 entry
8. The TODO comment in code matches: "EXPERIMENTAL v1 -- behavior signature design is NOT final. See .planning/DECISIONS.md"
</verification>

<success_criteria>
Given any (num_steps, pop_size, 2) agent_positions array, extract_behavior_signature() returns a (pop_size, 169) float32 array. The function passes jit.lower().compile() without error. Signatures for qualitatively different levels (sparse vs. dense agent coverage) are visually distinct. DECISIONS.md records the design rationale so collaborators know this is a revisable v1 implementation.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-foundation-02-SUMMARY.md with:
- Functions added to es/regret_fitness.py
- JIT compatibility test result
- Visual distinctness test result (L1 distance observed)
- Files created
- Any issues encountered
</output>
