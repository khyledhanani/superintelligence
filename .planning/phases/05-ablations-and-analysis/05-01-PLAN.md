---
phase: 05-ablations-and-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - accel_training/train.py
  - accel_training/config.yml
  - tests/test_phase3_ns_es.py
  - tests/test_phase4_sv_cma_es.py
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "train.py has exactly two modes: replay and es_step; no MAP-Elites archive, no archive warm-up, no mutate branch"
    - "All three ES strategies (cma_es, ns_es, sv_cma_es) run 3 updates without error under the new two-mode architecture"
    - "Bootstrap loop fills buffer to bootstrap_min levels via es_step before any replay occurs"
    - "WandB init uses name=run_name and group=wandb_group (not group=run_name)"
    - "config.yml has bootstrap_min and replay_ratio keys; use_accel, n_candidates, mutation_sigma, random_fraction, warmup_n are removed"
    - "Existing test files updated to match new train() return signature (train_state only, not tuple)"
  artifacts:
    - path: "accel_training/train.py"
      provides: "Two-mode training pipeline (replay / es_step) with bootstrap loop"
      contains: "bootstrap_min"
    - path: "accel_training/config.yml"
      provides: "Updated config with bootstrap_min, replay_ratio, removed ACCEL keys"
      contains: "bootstrap_min"
    - path: "tests/test_phase3_ns_es.py"
      provides: "Updated Phase 3 tests for new train() signature"
    - path: "tests/test_phase4_sv_cma_es.py"
      provides: "Updated Phase 4 tests for new train() signature"
  key_links:
    - from: "accel_training/train.py"
      to: "accel_training/es_components"
      via: "ES strategy routing (cma_es / ns_es / sv_cma_es)"
      pattern: "es_strategy.*sv_cma_es|ns_es|cma_es"
    - from: "accel_training/train.py"
      to: "accel_training/config.yml"
      via: "config dict loading"
      pattern: "bootstrap_min|replay_ratio"
    - from: "tests/test_phase3_ns_es.py"
      to: "accel_training/train.py"
      via: "import train"
      pattern: "from accel_training.train import train"
---

<objective>
Rewrite accel_training/train.py from scratch as a clean two-mode pipeline (replay / es_step), update config.yml to remove obsolete keys and add new ones, and update existing test files to match the new train() return signature.

Purpose: The current train.py is a 732-line three-branch loop (new / replay / mutate) with a MAP-Elites Archive threaded throughout. Phase 5 simplifies this to exactly two modes — replay (PLR buffer -> agent training) and es_step (ES ask -> VAE decode -> eval -> PLR insert -> ES tell) — removing ~200 lines of archive/mutation machinery. This clean architecture enables fair four-way comparison experiments.

Output: Rewritten train.py, updated config.yml, updated test files — all three ES strategies verified running under the new architecture.
</objective>

<execution_context>
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/workflows/execute-plan.md
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ablations-and-analysis/05-RESEARCH.md

# Current train.py (the file being rewritten — read for reference, then replace entirely)
@accel_training/train.py
@accel_training/config.yml

# ES strategies (unchanged — these are the consumers of the new architecture)
@accel_training/es_components/__init__.py
@accel_training/es_components/interface.py
@accel_training/es_components/nses_strategy.py
@accel_training/es_components/svcmaes_strategy.py

# Tests that need updating (read to understand current structure)
@tests/test_phase3_ns_es.py
@tests/test_phase4_sv_cma_es.py

# Functions used by train.py (unchanged)
@accel_training/ued_interface.py
@accel_training/es_components/regret_fitness.py

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From accel_training/es_components/__init__.py:
- CMAESStrategy: init_state(rng, config) -> state; ask(state, rng) -> (latents, state); tell(state, latents, fitness) -> state
- NSESStrategy: init_state(rng, config) -> state; ask(state, rng) -> (latents, state); tell(state, latents, fitness, buffer_sigs, valid_mask, config) -> (state, mean_novelty)
- SVCMAESStrategy: init_state(rng, config) -> state; ask(state, rng) -> (latents, state); tell(state, latents, fitness, behavior_sigs, config) -> (state, metrics_dict)

From accel_training/ued_interface.py:
- load_vae(checkpoint_path) -> vae_model
- build_eval_fn(vae_model, env, env_params, config) -> eval_fn
  # eval_fn(rng, params, latents) -> (sequences, levels, regrets, max_returns, valid)

From accel_training/es_components/regret_fitness.py:
- rollout_agent_on_levels_with_positions(rng, env, env_params, agent_params, network, levels, num_steps) -> (obs, actions, rewards, agent_positions)
- extract_behavior_signature(agent_positions, num_steps) -> behavior_sigs  # (batch, 169) L1-normalized

From jaxued (LevelSampler):
- level_sampler.initialize(levels, level_extra) -> sampler
- level_sampler.insert_batch(sampler, levels, scores, level_extra) -> (sampler, inserted_mask)
- level_sampler.sample_replay_levels(sampler, rng) -> (sampler, level_inds, replay_levels)
- level_sampler.sample_replay_decision(sampler, rng) -> should_replay (bool)
- level_sampler.update_batch(sampler, level_inds, scores, level_extra) -> sampler
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean rewrite of train.py as two-mode pipeline</name>
  <files>accel_training/train.py</files>
  <action>
Rewrite accel_training/train.py FROM SCRATCH. Read the current file for reference first, then replace entirely. The new file implements the two-mode architecture described below.

**Structure of new train.py:**

```
train(config)
├── Setup: env, VAE, eval_fn, WandB, ES strategy, LevelSampler, agent (TrainState)
├── Bootstrap loop: run es_step until sampler["size"] >= bootstrap_min
│     Internally: ES ask() -> eval_fn() -> behavior_sig -> insert_batch() -> ES tell()
├── Main loop (for update in range(num_updates)):
│     ├── Decide mode:
│     │     if sampler["size"] < bootstrap_min -> es_step (forced)
│     │     else -> replay with probability replay_ratio, else es_step
│     ├── es_step branch:
│     │     ES ask() -> latents
│     │     eval_fn(rng, params, latents) -> sequences, levels, regrets, max_returns, valid
│     │     rollout_agent_on_levels_with_positions() -> behavior_sigs
│     │     Solvability gate: filter valid levels only
│     │     insert_batch(sampler, levels, regrets, level_extra)
│     │     ES tell() [strategy-specific: CMA-ES / NS-ES / SV-CMA-ES]
│     │       For SV-CMA-ES: second eval pass with repulsion (same logic as current lines 518-591)
│     └── replay branch:
│           sample_replay_levels() -> level_inds, replay_levels
│           _train_on_levels(rng, train_state, replay_levels) -> new_scores, losses
│           update_batch(sampler, level_inds, new_scores, updated_extra)
└── Logging, checkpointing (agent params only — no archive arrays)
```

**KEEP from current train.py** (transplant these, adapted to new structure):
- All imports that are still needed (jax, flax, optax, distrax, wandb, jaxued, es_components, ued_interface, regret_fitness)
- `_train_on_levels()` JIT-compiled helper (exact same function — pure PPO training on given levels)
- TrainState namedtuple/dataclass
- `_save_checkpoint()` function — simplified: saves agent_params.pkl only (NO archive_envs.npy, NO archive arrays)
- argparse block at bottom (main entry point), but remove --use_accel flag; keep --n_particles, --config, --seed, --num_updates, --log_dir, --run_name, etc.
- The solvability gate pattern: valid_np = np.asarray(valid) & ~nan_mask; valid_indices = np.where(valid_np)[0]
- The behavior_sig extraction pattern: separate rollout_agent_on_levels_with_positions call after eval_fn; extract_behavior_signature on agent_positions
- Three-way ES routing at init: cma_es -> CMAESStrategy, ns_es -> NSESStrategy, sv_cma_es -> SVCMAESStrategy
- Three-way ES tell() logic: CMA-ES (simple tell), NS-ES (tell with buffer_sigs + valid_mask), SV-CMA-ES (two-pass eval with Stein repulsion + second insert_batch)
- WandB define_metric and per-update logging (regret, novelty, buffer stats, sv_behavior_dist_pre/post)
- CSV logging as secondary output

**REMOVE entirely** (do NOT carry over):
- `from archive import Archive` import
- `from ued_interface import generate_candidates, mutate_latents, update_archive` imports
- `run_archive_warmup()` function (entire function — ~78 lines)
- `UpdateState` IntEnum (NEW/REPLAY enum)
- `last_replay_latents` state variable
- `use_accel` config key usage
- Any code referencing `archive` object (instantiation, update, checkpoint save, logging)
- `n_candidates`, `mutation_sigma`, `random_fraction` config reads
- `warmup_n` config reads (replaced by bootstrap_min)
- Archive-related checkpoint saves (archive_envs.npy etc.)
- The old three-branch if/elif/else (new/replay/mutate) — replaced by two-mode if/else (es_step/replay)

**NEW elements:**
- `bootstrap_min = config.get("bootstrap_min", 50)` — minimum buffer size before replay allowed
- `replay_ratio = config.get("replay_ratio", 0.8)` — probability of replay mode vs es_step
- Bootstrap loop before main loop: run es_step iterations until sampler["size"] >= bootstrap_min
- Mode decision: `level_sampler.sample_replay_decision(sampler, rng)` returns True/False; use it if buffer >= bootstrap_min
- WandB init corrected: `name=config["run_name"], group=config.get("wandb_group", "phase5-comparison")` (NOT group=run_name)
- `train()` returns `train_state` only (NOT tuple of (train_state, archive))
- Add `--group` argparse argument forwarded to config["wandb_group"]
- Add `--es_strategy` argparse argument forwarded to config["es_strategy"]

**Critical implementation details:**
- The LevelSampler is initialized with `replay_prob=config.get("replay_ratio", config.get("replay_prob", 0.8))` — backward compatible with old config key
- behavior_sig placeholder shape is 169 (13x13 grid histogram)
- The solvability gate MUST filter valid levels before insert_batch (same pattern as current code)
- For SV-CMA-ES two-pass eval: keep the exact same logic as current train.py lines 518-591 (first pass -> Stein repulsion -> second eval -> second insert_batch)
- The `_train_on_levels()` function is transplanted exactly as-is — it is a pure PPO training step
- Checkpoint saves agent params only (pickle). No archive arrays.
  </action>
  <verify>
    <automated>/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python -c "from accel_training.train import train; print('import OK')"</automated>
  </verify>
  <done>
    - train.py is a complete rewrite with two-mode pipeline (replay / es_step)
    - No references to Archive, generate_candidates, mutate_latents, update_archive, UpdateState, run_archive_warmup
    - Bootstrap loop fills buffer via es_step before main loop
    - WandB init uses name=run_name, group=wandb_group
    - train() returns train_state (not tuple)
    - All three ES strategies routable via config["es_strategy"]
  </done>
</task>

<task type="auto">
  <name>Task 2: Update config.yml and test files for new architecture</name>
  <files>accel_training/config.yml, tests/test_phase3_ns_es.py, tests/test_phase4_sv_cma_es.py</files>
  <action>
**config.yml changes:**

1. Remove these keys (they belong to the old ACCEL/MAP-Elites architecture):
   - `use_accel` (and its comment)
   - `n_candidates` (and its comment)
   - `mutation_sigma` (and its comment)
   - `random_fraction` (and its comment)
   - `warmup_n` (and its comment)
2. Rename `replay_prob` to `replay_ratio` (same value: 0.8). Update comment to: "Probability of replay mode vs es_step mode"
3. Add new key: `bootstrap_min: 50` with comment: "Minimum buffer levels before replay allowed (replaces archive warm-up)"
4. Add new key: `wandb_group: phase5-comparison` with comment: "WandB group tag for experiment comparison"
5. Update the section header comment "# --- ACCEL / MAP-Elites ---" to "# --- ES / Level Generation ---" since MAP-Elites is removed
6. Keep `eval_rollout_steps`, `min_obstacles`, `min_distance`, `decode_temperature` under the new section header

**tests/test_phase3_ns_es.py changes:**

1. Find `test_archive_warmup_populates_buffer` function — REPLACE it with `test_bootstrap_populates_buffer`:
   - Instead of importing `run_archive_warmup`, test the bootstrap behavior directly:
   - Create a minimal config with `bootstrap_min=10`, `num_updates=0`, `es_strategy="ns_es"`
   - Call `train(config)` with num_updates=0 (or a very small number like 3)
   - After train returns, the bootstrap should have run, populating the buffer
   - Since we cannot inspect sampler post-return easily, simplify: assert train(config) completes without error and returns a train_state (not a tuple)
   - Actually: the simplest correct test is to verify `train(config)` does NOT crash with the new config keys — that proves bootstrap works

2. Find the end-to-end smoke test (`test_end_to_end_3_updates` or similar) — update:
   - Change `train_state, archive = train(config)` to `train_state = train(config)`
   - Remove any assertions about `archive` (e.g., `archive.num_filled`)
   - Remove config keys that no longer exist: `use_accel`, `n_candidates`, `mutation_sigma`, `random_fraction`, `warmup_n`
   - Add config keys: `bootstrap_min: 5`, `replay_ratio: 0.5` (low bootstrap for fast test)
   - Keep `es_strategy: "ns_es"` and all ES-specific config keys

3. Remove the `run_archive_warmup` import from the file header

**tests/test_phase4_sv_cma_es.py changes:**

1. Find `test_end_to_end_3_updates_sv_cma_es` — update:
   - Change `train_state, archive = train(config)` to `train_state = train(config)`
   - Remove any assertions about `archive`
   - Remove obsolete config keys: `use_accel`, `n_candidates`, `mutation_sigma`, `random_fraction`, `warmup_n`
   - Add config keys: `bootstrap_min: 5`, `replay_ratio: 0.5`
   - Keep `es_strategy: "sv_cma_es"`, `sv_n_particles`, `sv_epsilon`

**After all edits, run all four test files** to verify nothing is broken:
```bash
PYTHON=/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python
$PYTHON tests/test_es_components.py
$PYTHON tests/test_phase3_ns_es.py
$PYTHON tests/test_phase4_sv_cma_es.py
```
  </action>
  <verify>
    <automated>/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python tests/test_phase3_ns_es.py && /cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python tests/test_phase4_sv_cma_es.py</automated>
  </verify>
  <done>
    - config.yml has bootstrap_min and replay_ratio; use_accel, n_candidates, mutation_sigma, random_fraction, warmup_n removed
    - test_phase3_ns_es.py: no import of run_archive_warmup; smoke test uses train_state = train(config); all tests pass
    - test_phase4_sv_cma_es.py: smoke test uses train_state = train(config); all tests pass
    - test_es_components.py still passes (unchanged, sanity check)
  </done>
</task>

</tasks>

<verification>
1. `grep -c "archive\|Archive\|run_archive_warmup\|UpdateState\|use_accel\|n_candidates\|mutation_sigma\|random_fraction\|warmup_n" accel_training/train.py` returns 0 — all old references removed
2. `grep -c "bootstrap_min\|replay_ratio" accel_training/train.py` returns >= 2 — new config keys in use
3. `grep "name=config\|group=config" accel_training/train.py` shows correct WandB init pattern
4. All three test files pass without ImportError or ValueError
5. `python -c "from accel_training.train import train; print('OK')"` succeeds
</verification>

<success_criteria>
- train.py is a clean two-mode pipeline under 500 lines (reduced from 732)
- Zero references to Archive, MAP-Elites, warm-up, mutate branch
- All three ES strategies (cma_es, ns_es, sv_cma_es) verified via passing test smoke tests
- config.yml reflects the new architecture with no dead keys
</success_criteria>

<output>
After completion, create `.planning/phases/05-ablations-and-analysis/05-01-SUMMARY.md`
</output>
