---
phase: 06-run-pca-space-cma-es-on-tpu-30k-steps-5-seeds-and-compare-solve-rate-against-phase-4-results
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: []
autonomous: false
requirements: [RUN-02, RUN-03]

must_haves:
  truths:
    - "WandB group pca-cmaes-accel has exactly 5 completed runs at 30k updates in project JAXUED_COMPARISON"
    - "scripts/compare_phase4_results.py produces a 3-way table: PCA-CMA-ES vs CMA-ES-CNN-VAE vs ACCEL baseline with mean +/- std"
    - "solve_rate/mean is reported for all three groups (no NO DATA rows)"
    - "Comparison result is recorded in .planning/phases/06-.../06-RESULTS.md for the write-up"
  artifacts:
    - path: ".planning/phases/06-run-pca-space-cma-es-on-tpu-30k-steps-5-seeds-and-compare-solve-rate-against-phase-4-results/06-RESULTS.md"
      provides: "Final comparison table and Phase 6 conclusion"
      contains: "pca-cmaes-accel"
  key_links:
    - from: "scripts/compare_phase4_results.py"
      to: "WandB JAXUED_COMPARISON"
      via: "wandb.Api().runs(project, filters={'group': group})"
      pattern: "api.runs"
    - from: "06-RESULTS.md"
      to: "Phase 4 WandB data"
      via: "compare_phase4_results.py output"
      pattern: "solve_rate"
---

<objective>
After the 5-seed PCA-CMA-ES experiment completes (~10-20 hours after Plan 02), retrieve results from WandB and produce the final 3-way comparison table. Write the results to a RESULTS.md file for the project write-up.

Purpose: This is the terminal plan for Phase 6 and the entire CNN-VAE integration project. The comparison table is the primary deliverable: does PCA-space CMA-ES improve solve_rate over full-64-dim CMA-ES?
Output: Comparison table in `.planning/phases/06-.../06-RESULTS.md` with final `solve_rate/mean` for PCA-CMA-ES, CMA-ES-CNN-VAE, and ACCEL baseline.
</objective>

<execution_context>
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/workflows/execute-plan.md
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-run-pca-space-cma-es-on-tpu-30k-steps-5-seeds-and-compare-solve-rate-against-phase-4-results/06-RESEARCH.md

<interfaces>
<!-- WandB group names — must match exactly -->
Groups in JAXUED_COMPARISON:
  "pca-cmaes-accel"       -- Phase 6 (5 seeds x 30k updates, --use_pca_search)
  "cmaes-cnn-vae-accel"   -- Phase 4 (5 seeds x 30k updates, full 64-dim CMA-ES)
  "accel-baseline"        -- Phase 4 (5 seeds x 30k updates, vanilla ACCEL)

Key metric: solve_rate/mean (final value at update 30000)

WandB API usage:
  api = wandb.Api()
  runs = api.runs(project_path, filters={"group": group})
  # run.history(keys=["solve_rate/mean"], x_axis="num_updates").iloc[-1]
  # Note: entity is the WandB username; use wandb whoami on TPU to confirm
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm all 5 PCA-CMA-ES seeds are complete</name>
  <files></files>
  <action>
Human verification required. Check WandB or TPU logs to confirm all 5 seeds in group pca-cmaes-accel have finished at 30k updates before proceeding to analysis. See how-to-verify for exact steps.
  </action>
  <verify>
    <automated>MISSING — requires human to verify WandB group pca-cmaes-accel shows 5 finished runs at 30k updates</automated>
  </verify>
  <done>WandB project JAXUED_COMPARISON shows 5 finished runs in group pca-cmaes-accel, each with num_updates reaching 30000</done>
  <what-built>
The PCA-CMA-ES experiment (5 seeds x 30k updates) was launched in Plan 02. This checkpoint confirms it has finished before running the comparison analysis.
  </what-built>
  <how-to-verify>
Check experiment completion via WandB or logs:

**Option A: WandB dashboard**
- Go to https://wandb.ai, project `JAXUED_COMPARISON`
- Filter by group `pca-cmaes-accel`
- Confirm 5 runs show state "finished" with num_updates reaching 30000

**Option B: TPU logs**
```bash
gcloud compute tpus tpu-vm ssh cma-es-v4 --zone us-central2-b
tmux attach -t pca_run
```
Look for "All PCA experiments complete." in the output, or check that all 5 seed logs exist:
```bash
ls -la ~/superintelligence/logs/pca_comparison_seed*.log
wc -l ~/superintelligence/logs/pca_comparison_seed*.log
```
All 5 log files should exist with substantial size (>10k lines each).

**If still running:** Do not proceed. Detach from tmux and check again later.
  </how-to-verify>
  <resume-signal>
Type "complete" when all 5 seeds have finished (WandB shows 5 finished runs in group pca-cmaes-accel). Type "partial N" if only N of 5 seeds completed. Describe any seeds that crashed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run comparison script and write 06-RESULTS.md</name>
  <files>.planning/phases/06-run-pca-space-cma-es-on-tpu-30k-steps-5-seeds-and-compare-solve-rate-against-phase-4-results/06-RESULTS.md</files>
  <action>
Run `scripts/compare_phase4_results.py` to query WandB and capture the output. Then write `06-RESULTS.md` with the comparison table and a brief conclusion.

**Step 1: Determine WandB entity**

First check the logged-in WandB user:
```bash
/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python -c "import wandb; api = wandb.Api(); print([r.entity for r in api.runs('JAXUED_COMPARISON')][:1])"
```

Or check via wandb CLI if available:
```bash
/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/wandb whoami 2>/dev/null || echo "use --entity flag"
```

**Step 2: Run the comparison script**

```bash
/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python \
  /cs/student/project_msc/2025/csml/gmaralla/superintelligence/scripts/compare_phase4_results.py \
  [--entity <entity-if-needed>]
```

Capture the full output. If it returns NO DATA for any group, try adding `--entity` with the correct WandB username.

**Step 3: Write 06-RESULTS.md**

Create the results file with:
- Date of completion
- The full comparison table output (paste verbatim)
- A one-sentence conclusion: did PCA-CMA-ES improve over CMA-ES-CNN-VAE?
- Any notable observations (e.g., Stage 2 activated at step 10k per WandB pca/stage metric)

Template:
```markdown
# Phase 6 Results: PCA-Space CMA-ES vs Phase 4 Baselines

**Completed:** <date>
**Experiment:** 5 seeds x 30k updates, TPU cma-es-v4
**WandB project:** JAXUED_COMPARISON

## Comparison Table

<paste compare_phase4_results.py output verbatim here>

## Conclusion

<one-sentence summary: did PCA-CMA-ES improve solve_rate over full-64-dim CMA-ES-CNN-VAE?>

## Notes

- Stage 2 (buffer PCA) activated at update 10k per WandB pca/stage metric
- Any seeds that required restart: <none or list>
- Total wall-clock time: <approx hours>
```

If the comparison script returns NO DATA for `pca-cmaes-accel` even with the correct entity, debug by listing all groups in the project:

```python
import wandb
api = wandb.Api()
runs = api.runs("JAXUED_COMPARISON")
groups = set(r.group for r in runs)
print("All groups:", groups)
```

Then re-run with the correct group name.
  </action>
  <verify>
    <automated>/cs/student/project_msc/2025/csml/gmaralla/miniconda3/envs/jax_env/bin/python -c "import ast; ast.parse(open('/cs/student/project_msc/2025/csml/gmaralla/superintelligence/scripts/compare_phase4_results.py').read()); print('compare script AST OK')" && test -f /cs/student/project_msc/2025/csml/gmaralla/superintelligence/.planning/phases/06-run-pca-space-cma-es-on-tpu-30k-steps-5-seeds-and-compare-solve-rate-against-phase-4-results/06-RESULTS.md && echo "RESULTS FILE EXISTS" && grep -qE "pca-cmaes-accel|solve_rate|Conclusion" /cs/student/project_msc/2025/csml/gmaralla/superintelligence/.planning/phases/06-run-pca-space-cma-es-on-tpu-30k-steps-5-seeds-and-compare-solve-rate-against-phase-4-results/06-RESULTS.md && echo "RESULTS CONTENT OK"</automated>
  </verify>
  <done>
    - `scripts/compare_phase4_results.py` runs without error and prints a comparison table with values (not "NO DATA" for all groups)
    - `06-RESULTS.md` exists with the comparison table pasted in and a conclusion line
    - solve_rate/mean values reported for at least pca-cmaes-accel and cmaes-cnn-vae-accel groups
  </done>
</task>

</tasks>

<verification>
Phase 6 complete when:
1. `06-RESULTS.md` exists with solve_rate/mean for all three groups
2. WandB shows 5 completed runs in group `pca-cmaes-accel` (verify via compare script output: n=5)
3. Comparison table shows PCA-CMA-ES result alongside Phase 4 baselines
4. Conclusion line states whether PCA-CMA-ES improves over full-64-dim CMA-ES
</verification>

<success_criteria>
- 5 seeds of PCA-CMA-ES completed at 30k updates (WandB: group pca-cmaes-accel, n=5)
- 3-way comparison table produced: PCA-CMA-ES vs CMA-ES-CNN-VAE vs ACCEL baseline
- `06-RESULTS.md` written with full table and conclusion (write-up artifact for the milestone)
- Phase 6 marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-run-pca-space-cma-es-on-tpu-30k-steps-5-seeds-and-compare-solve-rate-against-phase-4-results/06-03-SUMMARY.md` following the template at:
@/cs/student/project_msc/2025/csml/gmaralla/home/.claude/get-shit-done/templates/summary.md
</output>
